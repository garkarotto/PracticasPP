{% extends 'base.html.twig' %}

{% block title %}Profesor{% endblock %}

{% block body %}
    <h1>Profesor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ profesor.id }}</td>
            </tr>
            <tr>
                <th>Asignatura</th>
                <td>{{ profesor.Asignatura }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ profesor.Nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_profesor_index') }}">back to list</a>

    <a href="{{ path('app_profesor_edit', {'id': profesor.id}) }}">edit</a>

    {{ include('profesor/_delete_form.html.twig') }}
{% endblock %}
